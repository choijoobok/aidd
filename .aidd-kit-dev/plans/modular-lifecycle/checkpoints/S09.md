# S09 — 새 프로젝트·export·v2 배포 기준 정리

- 완료 기록: 2026-09-23T02:47:59+09:00
- 실행 승인: APR-013 — 사용자가 KIT-ADR-011의 축소 범위를 확인한 뒤 S09와 S10의 연속 수행 및 커밋·푸시를 승인했다.
- 단계 결과: 완료. 이전 AIDD 형식 전용 변환은 구현하지 않았고 신규 v2 배포 기준만 정리했다.

## 구현 결과

- `new-project`는 `owned-records-v2` 정본, 재생성 index와 현재 초기 정본의 파생 문서를 함께 만든다. `validate`와 `documentation-check`를 통과한 뒤에만 결과를 전달한다.
- 배포 검증은 v2 표식이 없거나 구형 전역 SSOT가 섞인 제품 출력, 퇴역한 자유형 `artifact.schema.json`을 거부한다.
- 폴더와 ZIP을 같은 staging에서 조립하고 각 파일 경로·내용 SHA256의 동등성을 smoke에서 비교한다. 기존 출력은 directory/ZIP/new-project 모두 덮어쓰지 않는다.
- `v2-distribution-contract.json`과 S09 회귀를 추가했다. 기존 `reference-project`는 schema_version 1 reader/generator 회귀 입력으로만 보존한다.
- 프로젝트 CI는 v2 생성 문서 전체 비교·validate·portable 훅 회귀를, Kit CI는 전체 관리 회귀와 smoke를 실행한다.
- S08의 S09 잔여 59개는 [S09 처리 기록](../integration/S09-disposition.json)으로 59/59 대응했다. 구형 fixture 53개는 회귀 입력으로 보존하고 6개 배포/도구/스켈레톤 자산은 갱신·검토·퇴역했다.

## 검증

- `node --test .aidd-kit-dev/tests/s09-distribution.test.mjs`: 3/3 통과.
- `node --test .aidd-kit-dev/tests/s08-assets.test.mjs .aidd-kit-dev/tests/s09-distribution.test.mjs`: 5/5 통과.
- `node .aidd-kit-dev/tools/kit.mjs check`: 통과.
- `node .aidd-kit-dev/tools/kit.mjs smoke`: 통과. 하위 Node 실행이 필요한 smoke는 sandbox EPERM 뒤 허용된 실행 환경에서 확인했다.
- `git diff --check`: 통과.

## 호환성·롤백

이전 AIDD 형식의 migration-plan/migration-build, 버전별 매핑과 변환 롤백은 KIT-ADR-011에 따라 제외했다. AIDD와 무관한 레거시 소스·문서 분석 기능은 유지한다. S09 변경을 되돌릴 때는 v2 배포 검증·초기 문서 생성·CI·스켈레톤·회귀만 되돌리고 기존 legacy fixture와 S08B 기능은 보존한다.

## 다음 단계

APR-013으로 S10 연속 수행이 승인됐다. 요구 21개·인수 기준 63개·시나리오 22개의 현재 적용 결과와 전체 회귀, 알려진 제한 및 읽기 전용 릴리스 계획을 확인한다.
